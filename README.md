# Qwen Chat

An interactive chat interface powered by Qwen AI. This is a simple, elegant web-based chat application.

## Features

- 💬 Real-time chat interface
- 🎨 Beautiful gradient UI with smooth animations
- 📱 Responsive design
- ⌨️ Keyboard support (Enter to send)
- ✨ Typing indicators and smooth transitions

## Deployment

This site is deployed via **GitHub Pages**.

### How to Deploy

1. **Create a GitHub Repository**
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   git branch -M main
   git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO_NAME.git
   git push -u origin main
   ```

2. **Enable GitHub Pages**
   - Go to your repository settings on GitHub
   - Scroll to "GitHub Pages" section
   - Select `main` branch as the source
   - Your site will be live at `https://YOUR_USERNAME.github.io/YOUR_REPO_NAME/`

3. **Or use a custom domain**
   - Add a `CNAME` file with your domain name
   - Configure DNS settings with your domain provider

## Local Development

Simply open `index.html` in your browser to test locally.

## Structure

- `index.html` - Main chat application (HTML, CSS, and JavaScript all-in-one)
- `README.md` - This file
- `.gitignore` - Git ignore rules

## License

MIT
